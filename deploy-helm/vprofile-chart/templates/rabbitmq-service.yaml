apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.rabbitmq.name }}
spec:
  ports:
  - port: {{ .Values.rabbitmq.servicePort }}
    targetPort: {{ .Values.rabbitmq.serviceTargetPort }}
    protocol: TCP
  selector:
    app: {{ .Values.rabbitmq.name }}
  type: ClusterIP